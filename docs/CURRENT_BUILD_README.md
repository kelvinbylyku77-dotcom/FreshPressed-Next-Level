# Current build

The September 2026 revision is in `site/`. Follow START_HERE.md to run or build it.

The existing redesign now includes official SVG branding, revised mobile layouts, video compositions, 21 coffee variants, a cart drawer/bottom sheet and an email acquisition offer UI. Home keeps four featured products; Shop has the full menu.

Current prices live in `site/menu.json`. `docs/MENU_DATA.json` is an unchanged historical snapshot. See PRICE_AUDIT.md for every item and the two unresolved tea entries. EMAIL_INTEGRATION.md describes the real subscription hook. No coupon is exposed or granted by the browser.

Earlier V3 files in `history/v3/` are for reference only. See PROJECT_HANDOFF.md for behavior and remaining Shopify integration.
